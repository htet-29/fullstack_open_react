{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Htet Hlaing Win",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js Haymar@2911999",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../../part2 && npm run build && cp -r dist ../part3/backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongoose": "^8.19.1",
    "morgan": "^1.10.1"
  }
}
